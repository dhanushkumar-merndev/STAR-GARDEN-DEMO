# Deployment tooling only — never loaded by the running Next.js application.
# Use the Supabase Dashboard's Database → Connection string → Session pooler
# URI when the direct database hostname is IPv6-only.
SUPABASE_DB_URL="postgresql://postgres.PROJECT_REF:PASSWORD@aws-0-REGION.pooler.supabase.com:5432/postgres"
